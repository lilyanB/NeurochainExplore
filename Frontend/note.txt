alors je résume(désoler si c'est redondant): j'ai mon index qui lance le router > le front affiche la page > le form envoie au router > le routeur envoie au controller > le controller a axios et renvoie au backend> puis chemin inverse pour afficher

c’est presque ça, le controller ne renvoie pas au routeur, il appelle directement la vue en donnant les données


c’est l’aspect “puis chemin inverse pour afficher” qui est plus direct en fait


okai donc: puis chemin a afficher = le routeur récupère directement le "return" de ce qu'il a appelé
Nouveau


C est plus l application que le router mais oui c est ça